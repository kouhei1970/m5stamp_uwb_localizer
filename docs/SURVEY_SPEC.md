# アンカー自動測量 仕様

## 目的
**アンカーを適当に設置したあと、巻尺での実測を「各アンカーの高さ5個」だけに減らし、
残りは自動で決めてタグの測位を開始できるようにする。**

## 確定した設計判断
| # | 決定 | 理由 |
|---|---|---|
| 1 | **ゲージ固定は Lv2（高さのみ実測）** | 垂直距離は巻尺で最も正確に測れる。5個で済む |
| 2 | **測量時はタグも6台目として参加** | 距離が 10→15本 に増え、共通アンテナ遅延の推定に余裕ができる |
| 3 | **計算は実機（ESP32-S3）上で行う** | PC 不要。現場完結 |
| 4 | 台間通信は **ESP-NOW** | AP/ルータ不要。2.4GHz なので UWB ch9 と電波干渉しない |
| 5 | 測量モードと運用モードは**排他** | WiFi スタックのジッタが遅延送信のウィンドウを乱すため |
| 6 | **高さも ToF 距離センサで自動計測（オプション）** | 巻尺を完全に不要にする。M5StampS3A の空き GPIO へ I2C を手配線して増設できる |

---

## 1. 推定するもの と 自由度

### 観測
6ノード（アンカー5 + タグ1）の総当たり相互測距 = **15本**
```
r_ij = d_ij + δ_i + δ_j     (i<j)
```

### 未知数
- 座標: 6ノード × 3 = **18**
- **共通アンテナ遅延 δ**（全ノード同一と仮定）: **1**
  → `r_ij = d_ij + 2δ`

台ごとに個別推定すると N=6 で 座標 18 − ゲージ 6 + 遅延 6 = 18 未知数 vs 15式で不足する。
同一製品・同一基板なので共通値とする。APS014（Qorvo のアプリケーションノート。アンテナ遅延校正の手順書）も
「30cm 以下の精度で良ければ台ごとの校正は不要」としている。

### 自由度の勘定（3D）
| 項目 | 数 |
|---|---|
| 観測（距離） | 15 |
| 座標の自由度 | 18 |
| ゲージ（並進3 + 回転3。鏡像は離散） | −6 |
| **実質の形状自由度** | **12** |
| 共通遅延 | +1 |
| **未知数計** | **13** |
| **冗長度** | **15 − 13 = 2** |

タグを入れないと 10本 vs 10未知数で余裕ゼロになる（これが判断2の根拠）。

### 実装で判明した制約

**(D) n=4 では共通遅延を推定できない。**
δ の識別可能性の必要条件は `m >= 3n-6+1`（十分条件ではない）。n=4 は最大6本 vs 必要7本で
常に不足する。実装は自動検出して **δ=0 に固定**する。

**(E) 冗長度0の構成は「残差0の別解」に落ちうる。【重要】**
6ノード2本欠け（13本 vs 未知数13）で、**残差RMS（Root Mean Square、二乗平均平方根）5.4e-16 のまま
座標2.74m違い・δ=−0.545（真値+0.08）** という解が実際に出た。
データを完璧に説明しているので**残差では検知できない**。

→ **冗長度 `リンク本数 − 未知数 + 6` を利用者に必ず表示し、0以下なら警告する。**
　 §5 の「残差RMSを見て採否を委ねる」ではこの失敗様式を捕まえられない。

**(F) 行列サイズは 25×25。**
`UWB_MAX_ANCHORS=8` に合わせると未知数は 3×8+1 = 25。
本文中の「19×19」は n=6 の場合の値。

---

## 2. 処理の流れ

```
[1] EDM 収集       ESP-NOW が指揮し、15本の相互測距を集める
[2] 初期配置       逐次三辺測量（ノード0 原点の Gram 行列をピボット付きコレスキー 3 段）
[3] 精密化         Levenberg-Marquardt で距離残差を最小化。共通遅延 δ も同時に推定
                   → 収束した形状を平面フィットと比べて縮退（同一平面・直線）を判定
[4] ゲージ固定     実測した高さに合わせて 回転(傾き2) + z並進 を決定
[5] XY 規約        アンカー0 を XY 原点、アンカー1 が +X 方向。鏡像は chirality 入力
                   （上から見て 0→1→2 が cw/ccw）で決め、無ければ規約で一意化
[6] 配信・保存     ESP-NOW で全台に配り、各自 NVS へ保存
```

実装は `components/uwb_survey/`（純計算、ESP-IDF 非依存）。線形代数はすべて
`components/uwb_math/`（スカラー展開した 3×3 と 3×3 ブロックのコレスキー）で済ませ、
**一般の固有分解・一般の密コレスキーは持たない**（ESP32-S3 は単精度 FPU のみで、
除算と sqrt が高い。`docs/archive/MATH_AUDIT_2026-08-21.md`）。

### [2] 初期配置の詳細（逐次三辺測量）

初期配置には逐次三辺測量を使う（古典的 MDS の二重中心化 → 8×8 Jacobi 固有分解は使わない）。
結果（LM の収束先）は同じで、固有分解が要らなくなる。

- 欠測リンクは最短経路（Floyd-Warshall）で埋める（MDS のときと同じ）
- ノード 0 を原点に取った Gram 行列 `G[i][j] = (d_0i² + d_0j² − d_ij²)/2`（i,j ≥ 1）は
  `G = X Xᵀ`（X は (n−1)×3 の座標）なので、**対角ピボット付きコレスキーを 3 段**回すと
  L の 3 列がそのまま座標になる:
  ピボット 1 = 原点から最も遠いノード（+X 軸）、ピボット 2 = その直線から最も遠い
  ノード（XY 平面）、ピボット 3 = その平面から最も遠いノード（+Z）。残りは
  4 球交差の三辺測量（`L[i][k] = G[i][pk] / L[pk][k]`）
- コスト: ≈ 300 flops + sqrt 3 + 除算 3（MDS は ≈ 20k flops + sqrt 数百）
- 縮退の一次判定: 3 段目のピボット値 / 1 段目 < 1e-5、または 2 段後の残差対角の
  総和から出す第 3 軸の RMS 広がり < 5cm。**ただしこれは生の距離 r = d + 2δ で
  見るので、同一平面 + δ≠0 は膨らんで見えて見逃す**。本判定は [3] のあと
- δ は初期値 0 のまま（全リンクが 2δ 伸びるだけなので形は保たれ、LM が縮める）

### [3] Levenberg-Marquardt の詳細
- 残差: `f_ij = |p_i − p_j| + 2δ − r_ij`（15本）
- 未知数ベクトル: `[p_0..p_5 (18), δ (1)]` = **19**（n=8 で 25）
- ヤコビ行の非零は 7 個（`+u`, `−u`, `2`）。正規方程式 `JᵀJ` は 3×3 ブロック構造
  なので **下三角ブロックパック + 縁取り 1 行（δ）**（`uwb_bchol`、n=8 で 349 要素）
  に直接積む（リンクあたり `u uᵀ` の 6 乗算 + 縁取り 3 + 1 = 10 乗算。密の 7×7 外積 49
  乗算より軽い）。解くのは 3×3 カーネルのブロックコレスキー（`uwb_bchol_factor/solve`）
- ゲージの不定性（並進3 + 回転3）は **LM のリッジ**で処理する（拘束式は足さない）。
  減衰の下限と絶対リッジは **計算機イプシロンに比例**（1e4·eps）させる。定数 1e-12 の
  決め打ちだと単精度でコレスキーが落ち続け（400 件で 377 回失敗・呼出 2.5 倍）、
  ゲージ方向の丸め誤差を 1e-12 で割った巨大な剛体移動が出る（レビュー A-9）
- 収束後、各ノードを隣接点の最良近似平面で鏡映して解き直し、コストが下がれば採用
  （flip ambiguity による局所解からの脱出）。平面の法線は 3×3 散布行列の最小固有
  ベクトル（閉形式）
- 外れ値リンクは **leave-one-out** で 1 本ずつ落とす（最大 2 本。冗長度 1 以上を保てる
  ときだけ）。詳細は [3''']

### [3'] 最終形状による縮退判定（レビュー A-1 の修正）

同一平面の配置では `∂d/∂z = 0` なので、5cm の測距誤差は `z ≈ sqrt(2dσ) ≈ 0.6m` の
見かけの厚みとして吸収され、LM は `ok=1` のまま z が裏返った座標を返していた
（δ=0.15 + 5cm ノイズで 200/200 回、最大誤差 1.5m）。初期配置の判定では捕まらない
（δ で三角形が膨らむ）ので、**LM 収束後の形状（δ 除去済み）**で判定し直す:

1. 散布行列の固有値（閉形式）。第 3 軸の RMS 厚み < 5cm、または λ2/λ0 < 1e-5 なら縮退
   （無雑音の同一平面・直線はここで決まる）
2. 形状を最良近似平面へ射影し、**面内だけで LM を解き直す**（初期値は「射影した 3 次元解」
   と「2 次元の逐次三辺測量」の 2 つを試して良い方）。その距離残差 RMS が
   `max(6cm, 3 × 3 次元フィットの残差 RMS)` 未満なら、「平面でも測距を同程度に説明できる
   = 第 3 次元は観測されていない」として縮退。
   6cm は σ=5cm の同一平面で平面残差の 99.9% 点（`σ·sqrt(χ²(dof2)/m)`）。
   **冗長度 0 ではこの段は行わない**（3 次元残差が必ず 0 で、2 次元モデルにも自由度が
   余らないため。呼び出し側が冗長度 0 を警告する前提）

この判定は「z が距離から観測できるか」を直接見るので、**80m × 3m × 2.4m のような
細長い配置も縮退扱いになる**（Δz²/(2d) ≈ 3cm しか距離に効かず、σ=5cm では z の誤差が
≈ 0.75m になる。20m × 3m × 2.4m は解ける）。本物の 3 次元配置（5m 級の部屋）では
n=6・σ=5cm で 1% 程度の偽陽性がある（同一平面の見逃しは 0.1% 未満）。偽陽性なら
測り直せば通る。

**根本対策（未実装）**: 実測高さを LM の観測に入れれば、同一平面でも z は高さで決まり、
縮退は「傾きが決まらない」問題に変わる。現状は [4] で高さを後付けしているので、
形状の z 構造は距離だけから来る。

### [3'''] 外れ値リンクの棄却（leave-one-out。レビュー A-2 の修正）

残差の MAD（中央絶対偏差）による判定は使わない。残差は外れ値込みの
最小二乗の後の値なので、冗長度が低いと外れ値が座標と δ に吸収されて残差が小さくなる
（マスキング）。公称 6 台構成（15 本 vs 未知数 13、冗長度 2）で (2,5) に +2m を入れても
残差は −0.16m（しきい値 0.30）で落とせず、座標 1.33m・δ −0.17m の誤りを `ok=1` で返す。

新しい判定は「そのリンクを外したらどれだけ説明できるようになるか」を直接試す:

1. 採用中の各リンク k について、k を外し、収束済みの全リンク解からウォームスタートで LM
   を回し、さらに鏡映探索（各ノードを隣接平面で反転して解き直す）を掛けて `cost_{−k}` を求める。
   鏡映探索が要るのは、2m 級の外れ値が全リンク解そのものを別の局所解（あるノードが反対側へ
   落ちた形。外れ値はほぼ吸収されコストは小さい）へ連れて行くことがあり、そこから k を外す
   だけでは元の形へ戻れないため（n=8 の +2m で 1/28、n=6 で 4/15 の位置で実測）
2. `Δ_k = cost_full − cost_{−k}`。Δ が最大のリンクが **(4σ)² = 0.04 m²（σ = 5cm）** を
   超えていれば落とし、全リンクで解き直して繰り返す（最大 2 本）

しきい値の根拠: 線形近似で `Δ_k = e_k²/(1−h_k)`（e_k は残差、h_k はてこ比）、外れ値が無ければ
`Δ_k ~ σ²·χ²(1)`。n=8（28 本）で 28 × P(χ²(1) > 16) ≈ 0.2%、n=6 は `Δ ≤ cost_full ~ σ²·χ²(2)`
で ≈ 0.03% の誤棄却（2000 試行の実測: n=6 0.05%、n=7 0.1%、n=8 0.35%）。
**σ はデータから推定せず 5cm を仮定する**。冗長度 2 では残差に 2 自由度しか無く
「無雑音 + 小さい外れ値」と「5cm ノイズ」を区別できないこと、MAD や他リンクの Δ の中央値は
マスキングで膨らむこと、外したあとの残差は 2 本目の外れ値で膨らむことが理由。
`cost_full ≤ 0.04` なら Δ もそれ以下なので leave-one-out は走らせない（外れ値の無い入力は
ほぼここで終わり、計算量の増加は無い）。

検出できる大きさ: `Δ_k ≈ (1−h_k)·ε²` なので `|ε| > 4σ / sqrt(1−h_k)`。平均の 1−h は
冗長度 / リンク数（n=6 で 0.13、n=8 で 0.32）。てこ比の高いリンクでは ±0.5m は落ちない
（n=6 では 15 本中 7 本。落ちなければ 0.1〜0.4m の座標誤差が残る）。±2m は n=7/8 の全位置、
n=6 では 15 本中 14 本で落ちる（残る 1 本は別の局所解が 4cm RMS で吸収してしまい、
`cost_full` が 0.03 でしきい値に届かない = 5cm ノイズと区別できない）。

限界（`uwb_survey_result.outlier_ambiguous` で知らせる）:
- **冗長度 2 では誰が外れ値かの同定が当てにならない。** 1 本外すと拘束が 1 本しか残らず、
  無関係なリンクを外しても外れ値は数 mm まで吸収される。無雑音なら区別できるが、5cm
  ノイズ下の +2m は 200 試行で正答 100 / 取り違え 94（n=7 は 197 / 3、n=8 は 198 / 2）。
  取り違えると残差は小さく見えるのに座標は 2m 誤る。次点の候補も測距ばらつきの範囲で
  説明できてしまうとき `outlier_ambiguous = 1` を立てるので、**立っていたらノードを増やして
  測り直す**
- 同じノードに外れ値が 2 本乗ると、そのノードを動かして両方を妥協させる方が安く、1 本ずつの
  greedy では捕まらない（総当たりの leave-two-out は高すぎるので採らない）
- 計算量: 1 掃引あたりコレスキー分解 n=6 で ≈2000 回、n=8 で ≈5000 回（旧 MAD 方式の
  300〜800 回に対し）。設置時 1 回の処理として許容した

### [3.5] 高さの自動計測（オプション）

**M5StampS3A + StampS3 BreakOut に I2C（Inter-Integrated Circuit）の ToF 距離センサ
（Time-of-Flight 方式の測距センサ）を付けると、高さ実測も不要になる。**

#### GPIO の空き（M5StampS3A + StampS3 BreakOut の場合）

`boards/stamps3.h` の UWB 用ピン割り当て（既定 `BOARD_STAMPS3_UWB_PORT_CONFIG`）:

| 場所 | ピン | 用途 |
|---|---|---|
| BreakOut ヘッダ | G12 / G11 / G13 / G10 | SPI（SCK/MOSI/MISO/CS） |
| BreakOut ヘッダ | G6 / G7 / G8 | RST / IRQ / WAKEUP |
| **BreakOut の Grove ポート** | **G13 / G15** | **G13 は上記 MISO と衝突するため使用不可** |
| BreakOut ヘッダ（空き） | G1〜G5 など | I2C（SDA/SCL）を手配線する候補 |

**⚠ 「Grove に挿すだけ」にはならない。** BreakOut の Grove ポートは G13/G15 に配線されて
いるが、**G13 は `pin_miso`（SPI MISO）と衝突する**（`boards/stamps3.h` 冒頭コメント）。
ToF センサを増設する場合は Grove コネクタを使わず、**空いている G1〜G5 あたりへ
SDA/SCL を手配線する**こと（具体的な2本の割り当ては未確定。実験0〜1で決める）。

I2C はバスなのでアドレスが衝突しなければ増設できる（VL53L1X 系 = 0x29）。
M5StampS3A 自体はオンボード IMU・地磁気センサを持たないため、IMU との共用問題は
発生しない。
**SPI（Serial Peripheral Interface）/ RST / IRQ（Interrupt ReQuest、割り込み要求）/ WAKEUP を
一切削らずに済む。** IRQ はアンカー側の折り返し時間（R5/R6）に効くので、これを失わないのは重要。

#### センサ選定
| 候補 | 上限 | 判断 |
|---|---|---|
| Unit ToF (VL53L0X) | 2m | **天井付近から床を測ると足りない可能性** |
| **Unit ToF4M (VL53L1X)** | **4m** | **推奨。** 天井高 2.4〜3m に余裕を持って対応 |
| VL53L3CX | 3m | `stampfly_ecosystem` に `sf_hal_vl53l3cx` の実装あり（流用候補） |

#### 仕様
- ToF は**真下を向けて床までの距離**を測る。それがそのアンカーの高さになる
- **センサと UWB（Ultra-Wideband、超広帯域無線）アンテナの取り付けオフセット**を設定で持つ（既定 0）。
  `h = tof_distance + mount_offset`
- **全台に付けなくてよい。** 3台以上あれば傾きと z オフセットは決まる。
  センサ値と手入力の**混在を許す**
- **測距のばらつきを見て怪しければ警告**する。真下に家具があると誤る
- Kconfig でオプション扱い。無効時は現行通り手入力のみ
- 精度は数cm級（巻尺の1cmより粗い）。ゲージ固定（傾き・z オフセット）には十分

#### 手順への影響
```
survey height auto          ToF 搭載台の高さを一括取得
survey height <addr> <m>    センサ非搭載台のみ手入力
```
**全台に ToF を付ければ巻尺が完全に不要になる。**

### [4] 高さによるゲージ固定

実測した各アンカーの高さ `h_k`（床からの垂直距離）を使う。

**高さでは鏡像は決まらない。** 「反転あり/なしの両方を試し、高さ残差が小さい方を
採用する」という方式は**原理的に不可能**である。証明:

```
高さモデル   h_i = u·p_i + c        (u は上方向の単位ベクトル、c はオフセット)
反転 S       直交かつ det(S) = -1
反転後に u' = Su と取ると
    u'·(S p_i) = (Su)ᵀ(S p_i) = uᵀ SᵀS p_i = uᵀ p_i        (SᵀS = I)
```
**反転前後で実現できる高さの組は完全に一致する。**
高さは線形汎関数なので、キラリティ（掌性）を検出できない。

→ 実装は両方を計算して比較するが（診断のため）、**必ず引き分けになる**。
   引き分け時は規約（XY 正規化後、|y| 最大ノードの y を正にする）で一意化し、
   `mirror_resolved = 0` を返す。

#### 鏡像の解決には外部から1ビットの情報が要る
**採用: キラリティを直接問う。**
```
survey chirality cw|ccw
  「上から見下ろして、ノード0 → 1 → 2 が時計回りか反時計回りか」
```
設置した本人なら記憶で答えられる。高さの実測より簡単。
未指定なら鏡像不定のまま返し、**その旨を明示**する
（相対的な位置関係は正しいので、用途によってはそのまま使える）。

**実装状況: ソルバ入力は実装済み、コンソールコマンドは S3〜S5 で。**
`uwb_survey_input.chirality` に +1（ccw = (p1−p0)×(p2−p0) の z > 0）/ −1（cw）/ 0（未指定）を
入れる（レビュー A-4）。[5] の正規化後は p0 = 0、p1 = (x1, 0) なので判定量は `x1·y2`、
符号は **ノード 2 の y の符号**そのもの。指定があれば従来の |y| 規約は使わず、結果の
`mirror_resolved = 1` にする。`uwb_survey_result.chirality_margin` に「判定が裏返るまでの余裕
[m]」を返す: 指定時は |y2|（上から見た 0–1 直線からノード 2 までの距離）、未指定時は |y| の
1 位と 2 位の差。**測距ばらつき（数 cm）と同程度以下なら判定を信用しないこと**（0,1,2 が
上から見てほぼ一直線、または |y| 最大の 2 ノードが対称）。

従来規約の弱点: ノード 0,1 を長方形の対角に置くと残りの 2 隅が y=±w/2 で厳密に対称になり、
5cm ノイズで毎回裏返る（200 試行で 107:93）。`chirality=±1` なら 200/200 同じ向き。

#### 高さで決まるもの
- **傾き（回転2自由度）と z 並進**: 推定 z を `h_k` に最小二乗で合わせる。
  `min Σ (u·q_k − g_k)²  s.t. |u| = 1` は球面拘束付きの最小二乗で、
  `(M + νI) u = b` を `|u(ν)| = 1` になる ν について Newton 法で解く**厳密解**
  （Moré–Sorensen の割線方程式。データが剛体変換で厳密に説明できれば 1 反復）。
  近似の「`u = M⁻¹b` を正規化」では、高さ 1cm のノイズで
  傾きが最大 0.55°（5m 先で 5cm）ずれる（レビュー A-6）
- **高さの実測は4点以上必要。** 3点だと中心化後に2次元しか張らず、
  上方向 u の「3点平面の法線成分」は**大きさだけ決まり符号が決まらない**。
  しかも符号によって未実測ノードの z が変わるため、XY 規約では一致させられない。
  実測が3点以下なら形状のみ返し、傾きは未確定として扱う
  （`uwb_survey_result.frame_determined = 0`。M のランクで判定するので、4 点以上でも
  実測点が同一平面なら 0）

### [5] XY の規約
- アンカー0 の XY を原点
- アンカー1 が +X 軸上（y=0, x>0）
- **任意だが再現性のある座標系**。飛行制御には支障ない

---

## 3. ESP-NOW プロトコル

### 役割
- **コーディネータ**: 測量を指揮する1台。タグが兼ねる（PC に挿さっている想定）
- **参加ノード**: アンカー5台 + タグ自身

### フレーム（ブロードキャスト）
| 種別 | 向き | 内容 |
|---|---|---|
| `SURVEY_HELLO` | coord → all | 測量開始。参加ノードは自分の ID を返す |
| `SURVEY_HELLO_ACK` | node → coord | 自分の short_addr |
| `SURVEY_ASSIGN` | coord → all | 「次のイニシエータは addr=X、相手は addr=Y」 |
| `SURVEY_RESULT` | node → coord | 測距結果（addr ペア、距離 mm、成否、試行回数） |
| `SURVEY_DONE` | coord → all | 収集完了。測量モード終了 |
| `CONFIG_PUSH` | coord → all | 確定した座標テーブルと遅延を配信 |
| `CONFIG_ACK` | node → coord | NVS 保存完了 |

- **1リンクずつ順に指名する**（同時に複数が送信すると UWB が衝突する）
- 各リンクは**複数回測って中央値**を採る（回数は設定可能、既定5回）
- 失敗したリンクは `SURVEY_RESULT` で成否を返し、コーディネータが再試行

### 排他制御
- `survey start` で ESP-NOW を初期化、**測位ループは停止**
- `survey stop` / `SURVEY_DONE` で **ESP-NOW を deinit**（`esp_now_deinit` + `esp_wifi_stop`）してから運用モードへ
- **測量中は測位しない。運用中は ESP-NOW を上げない**

---

## 4. コンソールコマンド（追加分）

**コーディネータ（タグ）**
```
survey start                 測量モードへ。ESP-NOW 起動、参加ノードを集める
survey status                収集済みリンク数 / 未取得のペア
survey height <addr> <m>     アンカーの実測高さを入力（5台分）
survey solve                 初期配置 + LM + ゲージ固定を実行して結果を表示
survey push                  確定した座標と遅延を全台へ配信し NVS 保存
survey stop                  ESP-NOW を落として運用モードへ
survey abort                 破棄
```

**アンカー**
```
（測量は ESP-NOW 経由で自動。手動介入は不要）
survey info                  自分が参加した測距の成否
```

---

## 5. 異常系

| 事象 | 扱い |
|---|---|
| 参加ノードが6台に満たない | `survey status` で不足を表示。**5台でも解けるが冗長度0になる**旨を警告 |
| 一部のリンクが取れない（遮蔽） | 再試行。それでも駄目なら**そのリンクを欠測として LM で扱う**（初期値は最短経路補完から作る）。片方向だけ NaN/非正のときは有効な側だけ使う |
| 測距が外れ値（NLOS・多重波で数十 cm〜m 単位で長い/短い） | leave-one-out で 1 本ずつ落とす（最大 2 本、冗長度 1 以上を保てるときだけ。上記 [3''']）。`uwb_survey_result.excluded` に落としたリンク。(4σ)²、σ=5cm のしきい値なので、てこ比の高いリンクの ±0.5m は落ちない |
| **外れ値の同定が曖昧** | `uwb_survey_result.outlier_ambiguous = 1`（落としたリンクの次点も測距ばらつきの範囲で説明できた。冗長度 2 の 6 台構成ではほぼ必ず立つ）。残差が小さく見えても座標が m 単位で誤りうる。**ノードを増やして測り直すよう促す** |
| 配置が縮退（同一平面・直線・z が観測できない） | `uwb_survey_result.degenerate = 1`（`ok = 0`）。配置を変えるよう促す。初期配置と LM 収束後の両方で判定する（上記 [3']） |
| LM が収束しない | 反復上限で打ち切り、残差 RMS を表示して採否を利用者に委ねる |
| **冗長度が0以下** | **残差が0でも誤った解の可能性がある。必ず警告する**（上記 (E)）。`uwb_survey_result.redundancy`（= 採用リンク数 − (3n−6+1)）を見る。縮退判定の第 2 段も行われない |
| 高さ入力が4点未満 | 傾きが決まらない。**形状のみ返し**、その旨を明示（3点でも足りない。上記[4]）。`uwb_survey_result.n_heights` / `frame_determined` を見る |
| キラリティ未指定（`chirality = 0`） | **鏡像が不定のまま返す。** 相対的な位置関係は正しい（`mirror_resolved = 0`、|y| 規約で一意化）。`chirality_margin`（|y| の 1 位と 2 位の差）が数 cm 以下なら測り直すたびに裏返りうる |
| キラリティ指定（`chirality = ±1`）だが 0,1,2 が上から見てほぼ一直線 | 判定はするが `chirality_margin`（ノード 2 の 0–1 直線からの距離）が測距ばらつき以下なので信用できない。ノードの番号付けを変える（0,1,2 が太い三角形になるように）よう促す |
| 推定遅延が異常値 | `uwb_survey_result.delay_suspect`（|δ| > 0.3m）なら警告。配線・配置の誤り、または冗長度 0 の別解を疑う |

`redundancy` / `n_heights` / `frame_determined` / `delay_suspect` は診断フィールド
（レビュー A-3 / A-5 対応）。`outlier_ambiguous` / `chirality_margin`（結果）と
`chirality`（入力）は A-2 / A-4 対応で追加した。**firmware 側の呼び出しはまだ無い。**

---

## 6. 成果物の受け渡し

測量結果は既存の `uwb::AnchorEntry` テーブルにそのまま入る:
```cpp
struct AnchorEntry {
    uint16_t short_addr;
    float    pos[3];            // ← 測量結果
    float    antenna_delay_m;   // ← 推定した共通遅延（全台同値）
    bool     enabled;
};
```
`components/uwb_cfgstore/` の NVS（Non-Volatile Storage、ESP-IDF の不揮発設定ストレージ）保存と ESP-NOW 配信を通す。
**既にコンソールで手入力できる形になっているので、受け皿は完成している。**

---

## 7. 実装の分割

| # | 対象 | 内容 | 実機必要性 |
|---|---|---|---|
| S1 | `components/uwb_survey/` | 逐次三辺測量 + LM + ゲージ固定。**ESP-IDF 非依存の純計算**にしてホスト検証可能に | 不要 |
| S2 | `components/uwb_math/` | 小次元の線形代数（3×3 閉形式固有値・ブロックコレスキー）。測量専用の密行列ソルバは廃止 | 不要 |
| S3 | `components/uwb_espnow/` | ESP-NOW の薄い層。フレーム定義と送受信 | 実機で要確認 |
| S4 | `firmware/anchor` | イニシエータに回る測量モード | 実機で要確認 |
| S5 | `firmware/tag` | コーディネータ + コンソールコマンド | 実機で要確認 |
| S7 | `components/uwb_tof/` | I2C ToF（VL53L1X）ドライバ + 高さ取得。**オプション** | 実機で要確認 |
| S6 | `tests/host/survey/` | **合成データでの検証**（既知配置 → 距離生成 → 復元 → 誤差評価） | 不要 |

**S1/S2/S6 は実機なしで完結する。** 先にそこを固めてから S3-S5 に進む。

### S6 で検証すること
- 既知配置（非同一平面、5台+タグ）から理論距離を作り、復元誤差が数値誤差レベルであること
- 距離にノイズ（σ=5cm）を乗せたときの復元誤差
- **共通遅延を仕込んで、正しく推定されること**
- 欠測リンクがあるときの挙動
- 縮退配置（同一平面、直線状、同一平面 + δ + ノイズ）で正しくエラーになること
- 鏡像は高さでは決まらない（§2[4]）ので、**どちらの鏡像から出発しても規約側の同じ解に
  収束すること**（入力の並べ替え・ノイズで裏返らないこと）
- 高さ実測にノイズがあるとき、返すフレームの高さ残差が真値フレームの残差以下であること
  （球面拘束付き最小二乗の厳密解であることの検算）
- 外れ値リンク（n=6/7/8 × {+2, −2, +0.5}m × 3 位置、無雑音）が正しく落ち座標誤差 < 5cm・
  δ 誤差 < 2cm、レビュー A-2 の入力（n=6、(2,5)+2m）で `outlier_ambiguous=1`、2 本の外れ値
  （ノードを共有しないペア、n=7/8）、5cm ノイズだけの 200 試行で誤棄却 ≤ 2 回
- `chirality = ±1` で対称配置 + 5cm ノイズ 200 試行が全部同じ向き、未指定では裏返ること、
  0,1,2 がほぼ一直線で `chirality_margin` が小さいこと

---

## 8. 対象外（将来）
- 台ごとの個別アンテナ遅延推定（未知数が足りない。ノード数を増やせば可能）
- 複数タグ
- 測量結果の時間的な追跡（アンカーが動いた場合の再測量は手動）
- ESP-NOW の暗号化

---

## 9. メジャー実測値からのアンカー座標計算（閉形式・軽量版）

上記の §1〜§7 は ESP-NOW での総当たり相互測距 + MDS初期値 + Levenberg-Marquardt
（`uwb_survey_solve()`）を前提にした自己測量で、S3〜S5（ESP-NOW層・ファーム側の
配線）は本稿時点で未実装。**それとは別に**、「アンカーを設置したあと、巻尺で
測った少数の距離だけから、反復も無い閉形式（代数式そのもの）で座標を出す」軽量な
経路を用意した。台数が少ない・ESP32-S3上でLMのような反復計算を回したくない・
今すぐ巻尺だけで座標を出したい場合に使う。

### 9.1 座標系の規約
§2[5] と同じ規約を使う（アンカー登録テーブルの並び = `anchor list` の添字番号）。
- index 0 が XY 原点
- index 1 が +X 軸上（y=0, x>0）
- index 2 は **+y 側に固定**（規約。§2[4]の |y| 規約と違い、こちらは常に+y側に倒す）
- index k≥3 の y の符号は d(2,k) があればそれで判定（無ければ index2 と同じ+y側）

### 9.2 自己測量との違い
| | §1〜§7（自己測量） | §9（メジャー閉形式） |
|---|---|---|
| 入力 | 総当たり相互測距（ESP-NOW で自動収集） | 基準2台からの距離のみ（巻尺で手入力） |
| 高さの扱い | 実測高さに**最小二乗で合わせる**（傾き・z並進を推定） | 入力した z を**そのまま**出力の z 座標に使う |
| 計算 | MDS初期値 + Levenberg-Marquardt（反復） | 余弦定理・ピタゴラスの定理（閉形式、反復なし） |
| 外れ値検出・冗長度診断 | あり（leave-one-out 等） | なし（入力が少ないため冗長度が無い） |
| 実装 | `uwb_survey_solve()`（uwb_survey.h/.c） | `uwb_survey_tape_solve()`（uwb_survey_tape.h/.c） |

### 9.3 入力
- ノード数 n（3〜8）。**アンカー登録テーブルの現在の台数**をそのまま使う
  （テーブルの index 0..n-1 が測量の対象になる）
- 各ノードの高さ z[i] [m]。既定0。z[0] を0以外にしてもよい
  （「床からの高さ」等、絶対的な基準を直接入れたい場合）
- 対距離（斜距離）:
  - `d(0,1)` **必須**
  - `d(0,k)`, `d(1,k)` (k=2..n-1) **必須**
  - `d(2,k)` (k=3..n-1) **任意**。無いと y の符号（左右）が index2 と
    同じ+y側に倒され、`sign_unresolved` フラグが立つ

### 9.4 計算
1. 高さ補正: 斜距離 d と高さ差 Δz から水平距離 h を出す
   （`h_ij² = d_ij² − Δz²`。ピタゴラスの定理の逆算）
2. 基準線: index0=(0,0,z0)、index1=(h01,0,z1)
3. index k≥2: 余弦定理で x = (h01² + h0k² − h1k²) / (2·h01)、
   ピタゴラスの定理で |y| = sqrt(h0k² − x²)
4. y の符号: index2 は+y固定。index k≥3 は d(2,k) があれば、
   (x, ±y, z_k) から index2 までの3次元距離の予測値のうち実測 d(2,k) に
   近い方を採用

根号の中身がわずかに負（**1cm² 以内**）になったときは巻尺の読み取り誤差とみなし
0に丸めて計算を続ける（`clamped_pairs`/`clamped_nodes` で警告）。それを超える
負値は測定同士の矛盾（三角不等式違反など）としてエラーにする。

### 9.5 異常系
| 事象 | 扱い |
|---|---|
| アンカー登録テーブルが3台未満 / 9台以上 | `UWB_SURVEY_TAPE_ERR_N_RANGE`。3〜8台で使える |
| 必須の距離が未入力 | `UWB_SURVEY_TAPE_ERR_MISSING`。欠けている距離を**全部集めて**返す（`missing` ビットマスク。一問一答にしない） |
| d(0,1) の水平距離が0以下（高さ差がd(0,1)以上） | `UWB_SURVEY_TAPE_ERR_BASELINE`。+X軸が定義できない |
| 三角不等式違反・斜距離が高さ差より短い | `UWB_SURVEY_TAPE_ERR_INCONSISTENT`（`err_i`/`err_j` に該当ノード） |
| d(2,k) が無い | 座標は出るが `sign_unresolved` で該当ノードを警告（左右が逆かもしれない） |
| 根号がわずかに負（1cm²以内） | 0に丸めて続行。`clamped_pairs`（ペア）/`clamped_nodes`（ノード）で警告 |

### 9.6 コンソールコマンド
```
survey dist <i> <j> <meters>   メジャーで測った距離を記録（RAMのみ、NVSへ保存しない）
survey z <i> <meters>          アンカーの高さを記録（既定0、RAMのみ）
survey show                    記録済みの入力一覧 + 計算可能なら座標プレビュー
survey apply                   座標を計算してアンカー登録テーブルへ書き込む（save は別途必要）
survey clear                   survey の入力を全消去
```
`i`, `j` は `anchor list` と同じ添字。`survey apply` は登録テーブルの
`enabled` フラグには触れない（座標だけを決める機能なので、有効/無効の管理は
`anchor enable`/`disable` に委ねる）。書き込み後は `anchor set` 等と同じ
後処理（`evaluateMode()` 再評価 + `reinitEkf()`）を通す。

### 9.7 実装
| 対象 | 内容 |
|---|---|
| `components/uwb_survey/include/uwb_survey_tape.h`<br>`components/uwb_survey/src/uwb_survey_tape.c` | 閉形式計算の本体。ESP-IDF非依存・malloc不使用の C99（`uwb_survey.h`/`uwb_survey.c` の自己測量ソルバとは別ファイル） |
| `firmware/tag/main/tag_console.cpp` | `survey dist/z/show/apply/clear` コマンド |
| `tests/host/survey/test_survey_tape.c` | ホスト側検証（既知配置からの往復・高さ補正・符号解決・異常系） |

**設計メモ（実装時の判断）**:
- `firmware/tag/main/CMakeLists.txt` の `REQUIRES` に `uwb_survey` を追加した
  （`tag_console.cpp` が `uwb_survey_tape.h` を使うため。他コンポーネントには
  影響しない追加のみの変更）。
- ESP-NOW経由の自己測量（§3のフレーム）とは独立に動く。両方を実装した場合、
  排他制御（§3「排他制御」）は自己測量側のものがそのまま使え、こちらは
  ESP-NOWを一切使わないので測位ループとの排他は元々不要。
