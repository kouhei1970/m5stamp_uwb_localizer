/* uwb_survey — アンカー自動測量（相互測距だけからノード座標を復元する）
 *
 * docs/SURVEY_SPEC.md の [2]〜[5] に相当する **純計算部分**。
 * ESP-IDF にも FreeRTOS にも UWB チップにも依存しない。ホストで
 * `cc` だけでビルドできる（tests/host/survey/Makefile 参照）。
 *
 *   * **malloc を呼ばない。** 作業領域はすべてスタックの固定長配列
 *   * 依存は <math.h> / <string.h> と components/uwb_math（スカラー展開した
 *     3x3 / 3x3 ブロックの線形代数。uwb_real の typedef もここから借りる）
 *   * 正規方程式（最大 25x25 = 3*8+1 個の未知数）は 3x3 ブロックの
 *     下三角パック（uwb_bchol、倍精度で 3KB）で持ち、ブロックコレスキーで解く。
 *     一般の固有分解・一般の密コレスキーは使わない（ESP32-S3 は単精度 FPU
 *     のみで除算・sqrt が高いため）
 *
 * components/uwb_loc/ と同じ流儀で書いてある（将来 uwb_localizer の c/ へ
 * 還元できるように）。
 *
 * ------------------------------------------------------------------------
 * 何を解いているか
 *
 * N 台のノードを適当な場所に置き、総当たりで相互測距する。得られるのは
 *
 *     r_ij = d_ij + 2*delta        (i<j, d_ij = |p_i - p_j|)
 *
 * だけ。delta は全ノード共通のアンテナ遅延（片道ぶん、単位 m）で、
 * 往復で 2*delta 効く。ここから
 *
 *     p_0 .. p_{N-1}  (3N 個) と delta (1 個)
 *
 * を推定する。距離だけでは **並進3 + 回転3 + 鏡像1** ぶんが決まらない
 * （ゲージの不定性）ので、
 *
 *   - 実測した高さ h_k で 傾き2 + z並進1 を固定（[4]）
 *   - ノード0 を XY 原点、ノード1 を +X 軸上 に置く規約で
 *     ヨー1 + XY並進2 を固定（[5]）
 *   - 鏡像は **高さでは決まらない**（後述）ので、決まった規約で潰す
 *
 * ------------------------------------------------------------------------
 * 【重要】鏡像（キラリティ）は高さ実測では決まらない
 *
 * 仕様書 [4] は「反転あり/なしの両方で高さ残差を計算し、小さい方を採用」
 * としているが、これは**数学的に必ず引き分けになる**。
 *
 *   ある反射 S（直交・det=-1）で反転した配置 S*p に対し、高さの予測値は
 *   任意の単位ベクトル u を「上」として  z_k = u.(S p_k) + c = (S u).p_k + c。
 *   S は直交なので u -> S u は単位球面の全単射。つまり
 *   **反転前に実現できる高さの組と反転後に実現できる高さの組は完全に一致する**。
 *   よってどちらの鏡像でも高さ残差の最小値は同じ値になる。
 *
 * 実際、床に置いた3台と天井の1台という配置を z=0 面で反転し、x 軸まわりに
 * 180 度回すと、高さはすべて元通りで xy だけが鏡像になる。
 *
 * したがって本実装は
 *   - 仕様どおり両方の高さ残差を計算して比較し（診断のため）、
 *     有意差があれば小さい方を採る（理論上は起きない）
 *   - 引き分け（＝常にこちら）なら、呼び出し側が `uwb_survey_input.chirality`
 *     で渡した **1 ビットの事前知識**（上から見て ノード 0→1→2 が反時計回り
 *     か時計回りか）で鏡像を選び `mirror_resolved = 1`
 *   - chirality が未指定（0）なら **規約**で潰して `mirror_resolved = 0`。
 *     規約は「[5] の正規化後、|y| が最大のノード（添字最小優先）の y を
 *     正にする」。これにより **どんな初期値・どちらの鏡像から出発しても
 *     出力は必ず同じ**になる（再現性はある。ただし現実の左右が当たっている
 *     保証はなく、|y| 最大の 2 ノードが対称な配置 — ノード 0,1 を長方形の
 *     対角に置くと必ずそうなる — では測距ばらつきで毎回裏返る）
 * とする。`uwb_survey_result.chirality_margin` に「判定が裏返るまでの
 * 余裕 [m]」を返すので、測距ばらつき（数 cm）と比べて小さければ判定を
 * 信用しないこと。
 *
 * 現実の左右を確定させたいなら、距離と高さ以外の情報（方位、
 * 「アンカー2 は入口から見て右」といった1ビットの事前知識、など）が要る。
 * `chirality` はその 1 ビットを受け取る口である。
 *
 * ------------------------------------------------------------------------
 * 使い方
 *
 *     uwb_survey_input in;
 *     uwb_survey_result out;
 *     uwb_survey_input_init(&in, 6);
 *     in.dist[0][1] = in.dist[1][0] = 4.83;  in.have[0][1] = in.have[1][0] = 1;
 *     ...
 *     in.height[0] = 0.30; in.have_height[0] = 1;
 *     ...
 *     in.chirality = +1;   // 上から見て 0→1→2 が反時計回り（分かるなら）
 *     if (uwb_survey_solve(&in, &out) && out.ok) {
 *         use(out.pos[i]);  use(out.common_delay_m);
 *     }
 *
 * ------------------------------------------------------------------------
 * スタック使用量
 *
 * `uwb_survey_solve()` は正規方程式（3x3 ブロックの下三角パック）を
 * スタックに置く。倍精度で呼び出し全体の深さは **約 7.3KB**
 * （xtensa-esp32s3-elf-gcc 14.2 -O2 -fstack-usage の実測: solve 2496B
 * （loo_drop_link / shape_degenerate はインライン化）+ escape_local_minima
 * 656B（本解きと leave-one-out の 2 箇所から呼ぶので別フレーム）+ lm_run
 * 4032B（uwb_bchol 3KB + 作業配列）+ uwb_bchol_factor 96B + uwb_m3_chol 64B。
 * -DUWB_USE_FLOAT なら 1504 + 352 + 2048 + 80 + 48 = **約 4.0KB**）。
 * 旧実装（25x25 密行列 + 8x8 Jacobi）の 9.4KB / 5.0KB より少ないが、
 * 余裕を見て ESP-IDF では **12KB 以上**のスタックを持つタスクから呼ぶこと
 * （app_main の既定 3.5KB では足りない）。
 * グローバル変数は一切持たないので再入可能。
 *
 * 計算量: 外れ値の無い入力では LM 数反復 + 縮退判定で、コレスキー分解
 * 200〜400 回（n=6〜8、5cm ノイズ）。外れ値があると leave-one-out が
 * リンクごとに LM + 鏡映探索を回すので 1 掃引あたり n=6 で ≈2000 回、
 * n=8 で ≈5000 回になる（設置時 1 回の処理として許容した）。
 */
#ifndef UWB_SURVEY_H
#define UWB_SURVEY_H

/* uwb_real（既定 double、-DUWB_USE_FLOAT で float）と小次元の線形代数。
 * uwb_real の typedef は UWB_REAL_DEFINED で守られているので、uwb_loc.h と
 * 同じ翻訳単位に include しても衝突しない（uwb_loc.h 側にも同じガードが
 * ある前提。幅は両コンポーネントとも CONFIG_UWB_LOC_USE_FLOAT に従う）。 */
#include "uwb_math.h"

#ifdef __cplusplus
extern "C" {
#endif

/* ------------------------------------------------------------------ 寸法 */

/** 測量に参加できるノード数の上限。components/uwb_loc の Kconfig 既定
 *  （UWB_LOC_MAX_ANCHORS=8）に合わせてある。増やすと未知数が増え、
 *  正規方程式（3N+1 の二乗）が二次で膨らむので注意。 */
#ifndef UWB_SURVEY_MAX_NODES
#define UWB_SURVEY_MAX_NODES 8
#endif

/** 3次元の形状を距離だけから決めるのに必要な最小ノード数。
 *  4 未満では四面体が作れない。 */
#define UWB_SURVEY_MIN_NODES 4

/** リンク（ノード対）の最大本数 = N(N-1)/2。N=8 で 28。 */
#define UWB_SURVEY_MAX_LINKS \
    (UWB_SURVEY_MAX_NODES * (UWB_SURVEY_MAX_NODES - 1) / 2)

/** 未知数の最大個数 = 座標 3N + 共通遅延 1。N=8 で 25。 */
#define UWB_SURVEY_MAX_UNKNOWNS (3 * UWB_SURVEY_MAX_NODES + 1)

/* ------------------------------------------------------------------ 型 */

/** 測量の入力。 */
typedef struct {
    int      n;                                                   /**< ノード数 (4..UWB_SURVEY_MAX_NODES) */
    uwb_real dist[UWB_SURVEY_MAX_NODES][UWB_SURVEY_MAX_NODES];    /**< 相互測距 [m]。生の値
                                                                   *   （アンテナ遅延を含んだまま）を入れる */
    int      have[UWB_SURVEY_MAX_NODES][UWB_SURVEY_MAX_NODES];    /**< 1 = 測定あり、0 = 欠測 */
    uwb_real height[UWB_SURVEY_MAX_NODES];                        /**< 実測した高さ [m] */
    int      have_height[UWB_SURVEY_MAX_NODES];                   /**< 1 = 高さの実測あり */
    int      chirality;                                           /**< 鏡像（キラリティ）の事前知識。
                                                                   *   +1 = 上から見下ろして ノード 0→1→2 が
                                                                   *        **反時計回り**（(p1−p0)×(p2−p0) の z > 0）
                                                                   *   −1 = 時計回り
                                                                   *    0 = 不明（従来の |y| 規約で一意化。
                                                                   *        uwb_survey_input_init の既定）
                                                                   *   docs/SURVEY_SPEC.md §2[4] の
                                                                   *   `survey chirality cw|ccw` に対応する */
} uwb_survey_input;

/** 測量の結果。 */
typedef struct {
    uwb_real pos[UWB_SURVEY_MAX_NODES][3]; /**< 推定座標 [m]。i >= n は 0 */
    uwb_real common_delay_m;               /**< 推定した共通アンテナ遅延 [m]（**片道あたり**）。
                                            *   観測モデルは r = d + 2*common_delay_m。
                                            *   識別可能でない構成（後述）では 0 に固定される */
    int      ok;                           /**< 0 なら pos は意味を持たない。**必ず見ること** */
    int      iterations;                   /**< Gauss-Newton(LM) の受理反復回数の合計 */
    uwb_real residual_rms;                 /**< 距離残差 RMS [m]。大きい = 測距か配置を疑う */
    int      degenerate;                   /**< 1 = 配置が縮退（同一平面・直線）。このとき ok=0 */
    int      mirror_resolved;              /**< 1 = 鏡像が決まっている。高さ残差の比較では
                                            *   **理論上決まらない**（ヘッダ先頭の説明参照）ので、
                                            *   実質 `in->chirality != 0` で判定できたとき（= 0,1,2 が
                                            *   上から見て一直線でないとき）だけ 1 になる。
                                            *   0 のときは規約で一意化してあり、左右は不定 */
    unsigned long excluded;                /**< 外れ値として落としたリンクのビットマスク。
                                            *   ビット位置は uwb_survey_link_index(i,j)。
                                            *   判定は leave-one-out（そのリンクを外して解き直した
                                            *   ときのコスト減が (4σ)²、σ=5cm を超えるか）。
                                            *   冗長度が 0 になる棄却はしない */
    int      redundancy;                   /**< 冗長度 = 採用リンク数 − (3n−6 + δ推定なら1)。
                                            *   **0 なら残差 0 のまま別解に落ちうる**
                                            *   （docs/SURVEY_SPEC.md §1 (E)）。呼び出し側は
                                            *   0 以下のとき必ず警告すること。失敗時は 0 */
    int      n_heights;                    /**< 高さ実測の点数（have_height の数） */
    int      frame_determined;             /**< 1 = 傾き（回転 2 自由度）が高さから一意に決まった
                                            *   （高さ実測 4 点以上かつ同一平面でない）。
                                            *   0 なら形状と z 並進だけが意味を持ち、傾きは
                                            *   規約で埋めてある（3 点以下では鏡像 2 解が残る） */
    int      delay_suspect;                /**< 1 = |common_delay_m| > 0.3 m。配線・配置・測距の
                                            *   誤り（または冗長度 0 の別解）を疑う */
    int      outlier_ambiguous;            /**< 1 = 外れ値の同定が当てにならない。excluded で落とした
                                            *   リンクの次点の候補も、外せば測距ばらつきの範囲で
                                            *   データを説明できた（冗長度 2 の 6 台構成で外れ値が
                                            *   あると通常こうなる: 1 本外すと拘束が 1 本しか残らず、
                                            *   どのリンクを外しても残りが整合してしまう）。
                                            *   残差は小さく見えても座標が大きく誤っている可能性が
                                            *   あるので、ノード（リンク）を増やして測り直すこと */
    uwb_real chirality_margin;             /**< 鏡像の判定が裏返るまでの余裕 [m]（判定に使った量の
                                            *   絶対値）。chirality 指定時は [5] の正規化後の
                                            *   ノード 2 の |y|（= 上から見た 0–1 直線からの距離。
                                            *   (p1−p0)×(p2−p0) の z 成分を |p1−p0| で割ったもの）、
                                            *   未指定時は |y| の 1 位と 2 位の差。
                                            *   測距ばらつき（数 cm）と同程度以下なら、0,1,2 が
                                            *   ほぼ一直線（または対称配置）で判定は当てにならない。
                                            *   失敗時は 0 */
} uwb_survey_result;

/* ------------------------------------------------------------------ API */

/** 入力をゼロ埋めして n だけ設定する。have[][] / have_height[] / chirality も 0 になる。 */
void uwb_survey_input_init(uwb_survey_input *in, int n);

/**
 * 測量を解く。
 *
 * 手順は docs/SURVEY_SPEC.md §2 のとおり:
 *   [2] 欠測リンクを最短経路で補完 → 逐次三辺測量（ピボット付き
 *       コレスキー 3 段）で初期配置
 *   [3] Levenberg-Marquardt で距離残差を最小化（共通遅延も同時推定）
 *       → 外れ値リンクを leave-one-out で棄却（最大 2 本、冗長度 1 以上を保つ）
 *       → 収束した形状を主成分分析して縮退（同一平面・直線）を判定
 *   [4] 実測高さで 傾き2 + z並進1 を固定（球面拘束付き最小二乗の厳密解）
 *   [5] ノード0 を XY 原点、ノード1 を +X 軸上に置き、鏡像を in->chirality
 *       （未指定なら |y| 規約）で一意化する
 *
 * 解けたら 1 を返し out->ok にも 1 を書く。解けなければ 0。
 * 失敗時も out は必ずゼロ初期化されており、縮退が理由なら
 * out->degenerate = 1 と、その時点の生の座標（初期配置または LM 後の
 * 形状。フレームは任意）が入る（診断用）。
 *
 * 失敗する条件:
 *   - n が範囲外 / 引数が NULL
 *   - 測距が取れているリンクだけでは形状が決まらない
 *     （どれかのノードのリンクが 3 本未満、リンク総数が 3n-6 未満、
 *      リンクのグラフが非連結）
 *   - 配置が縮退（同一平面・直線）→ degenerate = 1。初期配置と LM 収束後の
 *     両方で見る。**後者が本判定**: 生の距離には共通遅延 2δ が乗っていて
 *     同一平面でも三角形が膨らんで見えるため、初期配置だけでは δ≠0 の
 *     同一平面を見逃す。LM は δ を同時推定するので収束後の形状で判定する
 *   - LM が破綻した（正規方程式が解けない、NaN が出た）
 *
 * 共通遅延の識別可能性: リンク本数 m が 3n-6+1 以上でないと delta は
 * 形状の自由度と縮退して決まらない。**n=4（最大 6 本 vs 必要 7 本）は
 * 常にこれに該当する**ので、その場合 delta は 0 に固定して座標だけを解く。
 */
int uwb_survey_solve(const uwb_survey_input *in, uwb_survey_result *out);

/** リンク (i,j) に対応する excluded のビット位置。i!=j なら順序は問わない。
 *  ノード数に依存しない並び（j*(j-1)/2 + i, i<j）なので、n が変わっても
 *  同じペアは同じビットになる。範囲外なら -1。 */
int uwb_survey_link_index(int i, int j);

#ifdef __cplusplus
}
#endif

#endif /* UWB_SURVEY_H */
